---
id: T-000095
title: "Kotlin: Post, Transaction, Journal, Balance, Value types"
status: done
priority: p1
type: code
effort: m
labels: [parity, kotlin, layer-2]
tags: []
owner: null
created: "2026-03-22T00:00:00Z"
updated: "2026-03-22T00:00:00Z"
depends_on: [T-000093]
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Implement the transaction model types: Item, Post, Xact (Transaction), Journal, Balance, and Value.

## Acceptance Criteria
- [x] Item base class with date, state (cleared/pending/uncleared), note, tags
- [x] Post class with account, amount, cost, flags (virtual, balanceVirtual, generated)
- [x] Xact (Transaction) class with date, payee, posts, finalize() for balance checking
- [x] Journal class that holds list of transactions and account tree
- [x] Balance class: multi-commodity balance (Map<String, Amount>)
- [x] Value sealed class: polymorphic wrapper (Empty, AmountValue, BalanceValue) with arithmetic
- [x] Transaction finalize() validates postings balance
- [x] Null amount inference: if one posting has no amount, infer from remainder
- [x] Multi-commodity: implicit exchange when all postings have explicit amounts in different commodities
- [x] Unit tests for Balance arithmetic and transaction finalization

## Notes
Reference implementations:
- `port/python/src/muonledger/item.py` — Base Item
- `port/python/src/muonledger/post.py` — Posting
- `port/python/src/muonledger/xact.py` — Transaction with finalize()
- `port/python/src/muonledger/journal.py` — Journal container
- `port/python/src/muonledger/balance.py` — Multi-commodity balance
- `port/python/src/muonledger/value.py` — Polymorphic value

### Kotlin-specific notes:
- Use `sealed class Value` with `data class AmountValue(val amount: Amount)`, `data class BalanceValue(val balance: Balance)`, `data object Empty`
- Use `enum class State { UNCLEARED, CLEARED, PENDING }`
- Post flags can use boolean properties or an EnumSet

### Key finalize() logic:
1. Separate postings into groups: real, balanced-virtual `[Account]`, virtual `(Account)`
2. For each group, sum amounts
3. If exactly one posting has null amount → infer it from remainder
4. If balance is multi-commodity and no null posting → implicit exchange (skip check)
5. If single commodity and sum ≠ 0 → error

## Implementation Plan
- [x] Create Item.kt with State enum and metadata
- [x] Create Post.kt with account, amount, cost, flags
- [x] Create Balance.kt as Map<String, Amount> with arithmetic operators
- [x] Create Value.kt as sealed class with operator overloads
- [x] Create Xact.kt with finalize() balance checking
- [x] Create Journal.kt as container
- [x] Write tests
