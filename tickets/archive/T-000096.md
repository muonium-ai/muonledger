---
id: T-000096
title: "Swift: Textual journal parser"
status: done
priority: p1
type: code
effort: l
labels: [parity, swift, layer-3]
tags: []
owner: null
created: "2026-03-22T00:00:00Z"
updated: "2026-03-22T00:00:00Z"
depends_on: [T-000094]
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Implement the textual journal parser that reads ledger-format files and produces a Journal with transactions.

## Acceptance Criteria
- [x] Parse transaction headers: `YYYY-MM-DD [*|!] Payee [| Note]`
- [x] Parse postings: `    Account  [$]Amount[.cents]`
- [x] Parse null amounts (inferred posting)
- [x] Parse negative amounts: `$-15.00`
- [x] Parse date with both `-` and `/` separators
- [x] Parse commodity amounts: `100 EUR`, `$50.00`, `10 AAPL`
- [x] Parse large amounts with comma separator: `$12,500.00`
- [x] Parse inline posting comments: `$25.00  ; lunch`
- [x] Parse transaction-level comments/metadata: `; Payee: Staples`
- [x] Parse virtual postings: `(Budget:Food)` and `[Budget:Emergency]`
- [x] Parse cost annotations: `@ $1.10` and `@@ $110.00`
- [x] Parse lot annotations: `{$150.00}` → derive cost = qty * price
- [x] Parse balance assertions: `= $950.00`
- [x] Parse directives: `alias`, `apply account`/`end apply account`, `comment`/`end comment`, `include`, `P` price
- [x] Parse automated transactions: `= Predicate`
- [x] Parse periodic transactions: `~ Period`
- [x] Handle file inclusion: `include other.dat`
- [x] Parser produces valid Journal with finalized transactions
- [x] Unit tests for parsing various journal formats

## Notes
Reference implementations:
- `port/python/src/muonledger/parser.py` — Full-featured textual parser (~1500 lines)
- `port/rust/src/parser.rs` — Rust parser (~1200 lines)

### Parser structure (line-by-line):
The parser reads the file line by line:

1. **Blank lines** → end current transaction context
2. **Lines starting with digit** → transaction header: `YYYY-MM-DD [*|!] Payee`
3. **Lines starting with whitespace** → posting within current transaction
4. **`alias X=Y`** → account alias mapping
5. **`apply account X`** → push prefix onto account stack
6. **`end apply account`** → pop prefix from account stack
7. **`comment`** → skip until `end comment`
8. **`include FILE`** → recursively parse another file
9. **`P DATE COMMODITY PRICE`** → price directive (store in journal)
10. **`= PREDICATE`** → automated transaction
11. **`~ PERIOD`** → periodic transaction
12. **Lines starting with `;` or `#`** → comment, skip

### Posting parsing:
```
    Account  Amount [@ CostPerUnit | @@ TotalCost] [{LotPrice}] [= AssertAmount] [; Comment]
```

When parsing amount:
- Commodity symbol can be prefix (`$50`) or suffix (`100 EUR`)
- Detect commodity style from first occurrence
- Strip comma thousands separators before parsing number

### Lot annotation cost derivation:
When `{$150.00}` is parsed and no explicit `@ cost`:
```
cost = |quantity| * lot_price
```

### apply account:
Maintain a stack of prefixes. When `apply account Assets` is active, account `Checking` becomes `Assets:Checking`.

## Implementation Plan
- [x] Create Parser.swift with line-by-line state machine
- [x] Implement transaction header parsing (date, state, payee)
- [x] Implement posting parsing (account, amount, cost, annotations)
- [x] Implement directive parsing (alias, apply account, comment, include, P)
- [x] Implement automated/periodic transaction parsing
- [x] Integrate with Journal.addTransaction() and finalize()
- [x] Write parser tests covering all 35 parity test journals
