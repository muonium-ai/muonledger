---
id: T-000092
title: "Swift: Project setup + core types (Amount, Commodity, Account)"
status: done
priority: p1
type: code
effort: m
labels: [parity, swift, layer-1]
tags: []
owner: null
created: "2026-03-22T00:00:00Z"
updated: "2026-03-22T00:00:00Z"
depends_on: []
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Set up the Swift package and implement core data types: Amount, Commodity/CommodityPool, and Account tree.

## Acceptance Criteria
- [x] `port/swift/Package.swift` exists with executable target `muonledger`
- [x] `swift build` succeeds in `port/swift/`
- [x] Amount type with exact decimal arithmetic (use Foundation's Decimal or a Fraction type)
- [x] Commodity type with symbol, precision, and formatting styles
- [x] CommodityPool for commodity registry/interning
- [x] Account type with hierarchical parent/children tree and fullname
- [x] Unit tests for Amount arithmetic (+, -, *, negate, comparison)

## Notes
Reference implementations:
- `port/python/src/muonledger/amount.py` — Amount with fractions.Fraction for exact math
- `port/python/src/muonledger/commodity.py` — Commodity with styles (prefix/suffix symbol, precision)
- `port/python/src/muonledger/account.py` — Hierarchical account tree with fullname resolution
- `port/rust/src/amount.rs` — BigRational-based amounts
- `port/rust/src/commodity.rs` — CommodityPool with bitflags styles
- `port/rust/src/account.rs` — Arena-based account tree

### Package.swift structure
```
port/swift/
├── Package.swift
├── Sources/
│   └── MuonLedger/
│       ├── main.swift          (placeholder: just exits 0)
│       ├── Amount.swift
│       ├── Commodity.swift
│       └── Account.swift
└── Tests/
    └── MuonLedgerTests/
        └── AmountTests.swift
```

### Amount requirements
- Exact decimal arithmetic (no floating point)
- Commodity association (amount has a commodity)
- Formatting: respects commodity style (prefix `$100.00` vs suffix `100 EUR`)
- Parsing: `$50.00`, `100 EUR`, `$-15.00`, `$12,500.00` (thousands separator)
- Operations: add, subtract, multiply by scalar, negate, abs, is_zero, comparison

### Commodity requirements
- Symbol (e.g., `$`, `EUR`, `AAPL`)
- Precision (decimal places)
- Style flags: prefix vs suffix, thousands separator, decimal comma
- CommodityPool: find_or_create(symbol) → returns same instance

### Account requirements
- Name and fullname (e.g., `Checking` with fullname `Assets:Checking`)
- Parent/children tree
- find_or_create_account("Assets:Checking:Savings") creates hierarchy
- depth() method

## Implementation Plan
- [x] Create Package.swift with swift-tools-version 5.9+, executable target
- [x] Implement Amount.swift with Decimal-based arithmetic
- [x] Implement Commodity.swift with CommodityPool
- [x] Implement Account.swift with tree structure
- [x] Write AmountTests.swift
- [x] Verify `swift build` and `swift test` pass
