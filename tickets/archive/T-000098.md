---
id: T-000098
title: "Swift: CLI with balance, register, print commands"
status: done
priority: p1
type: code
effort: m
labels: [parity, swift, layer-4]
tags: []
owner: null
created: "2026-03-22T00:00:00Z"
updated: "2026-03-22T00:00:00Z"
depends_on: [T-000096]
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Implement the CLI entry point with balance, register, and print commands, plus --begin/--end date filtering and account pattern filtering.

## Acceptance Criteria
- [x] `swift run muonledger -f test.dat balance` works
- [x] `swift run muonledger -f test.dat register` works
- [x] `swift run muonledger -f test.dat print` works
- [x] `swift run muonledger --version` prints version (for parity test discovery)
- [x] `swift build` produces working binary
- [x] Balance command: shows account tree with totals, separator line, grand total
- [x] Balance command: accounts with zero balance and single child are collapsed
- [x] Balance command: parent account prefix preserved when collapsing (apply account fix)
- [x] Register command: shows date, payee, account, amount, running total per posting
- [x] Print command: re-outputs parsed transactions in ledger format
- [x] `--begin DATE` filters to transactions on/after date
- [x] `--end DATE` filters to transactions before date
- [x] Account pattern argument filters by account name (substring/regex match)
- [x] All 35 parity tests pass for Swift

## Notes
Reference implementations:
- `port/python/src/muonledger/cli.py` — Python CLI with argparse
- `port/python/src/muonledger/commands/balance.py` — Balance tree walk with collapsing
- `port/python/src/muonledger/commands/register.py` — Register with running totals
- `port/python/src/muonledger/commands/print_cmd.py` — Print command
- `port/rust/src/main.rs` — Rust CLI with clap derive
- `port/rust/src/commands/balance.rs` — Balance implementation
- `port/rust/src/commands/register.rs` — Register implementation

### CLI argument parsing:
Use Swift's `ArgumentParser` package (swift-argument-parser) or manual argv parsing.
```
muonledger -f <file> <command> [options] [patterns...]
muonledger --file <file> <command> [--begin DATE] [--end DATE] [patterns...]
muonledger --version
```

### Parity test invocation:
The parity test system calls: `swift run muonledger -f test.dat balance [args...]`

### Balance output format (must match C++ ledger):
```
             $200.00  Assets
             $200.00    Checking
            $-200.00    Savings
--------------------
                   0
```
Key formatting rules:
- Amount right-aligned in ~20 char column
- Account indented by 2 spaces per depth level from display root
- Accounts with 0 balance and single child are collapsed (e.g., `Assets:Checking` instead of `Assets\n  Checking`)
- When parent is collapsed, children show full path from collapsed parent
- Separator line of dashes
- Grand total at bottom

### Register output format:
```
24-Jan-15 Grocery Store          Expenses:Food                $50.00       $50.00
                                 Assets:Checking             $-50.00            0
```
Key formatting rules:
- Date in YY-Mon-DD format (e.g., `24-Jan-15`)
- Payee truncated/padded to ~20 chars
- Account padded to ~25 chars
- Amount right-aligned
- Running total right-aligned (cumulative sum of all postings so far)

### Print output format:
```
2024-01-15 Grocery Store
    Expenses:Food              $50.00
    Assets:Checking           $-50.00
```

### Date filtering:
- `--begin DATE`: include transactions where xact.date >= parsed_date
- `--end DATE`: include transactions where xact.date < parsed_date
- Date format: YYYY-MM-DD

### Account filter:
- Positional args after command are account filter patterns
- Simple substring match on account fullname is sufficient for parity

## Implementation Plan
- [x] Add swift-argument-parser dependency to Package.swift (or use manual parsing)
- [x] Implement main.swift CLI with -f, --version, subcommands
- [x] Implement BalanceCommand.swift with tree walk and formatting
- [x] Implement RegisterCommand.swift with running totals
- [x] Implement PrintCommand.swift
- [x] Add --begin/--end date filtering
- [x] Add account pattern filtering
- [x] Test with all 35 parity test journals manually
- [x] Verify `python port/scripts/test_parity.py` shows swift 35/35
