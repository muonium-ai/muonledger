import Foundation

let args = CommandLine.arguments
if args.contains("--version") {
    print("muonledger 0.1.0 (swift)")
    exit(0)
}

// Placeholder - will be replaced with full CLI
print("muonledger swift port")
